# Public Transit Data Analysis Project

This repository contains an R Markdown project focused on analyzing transit usage patterns using multiple datasets from different years.  
The project includes data cleaning, preprocessing, visualization, and analytical steps to explore trends across stations and time periods.

## 📁 Project Structure
- **San-Francisco-Bike-share-Network.Rmd** — Main R Markdown file containing the full workflow, analysis, and outputs.
- *(Add more files here as the project grows)*

## 🔧 Requirements

Before running the project, ensure you have:

- **R (4.0 or later)**
- **RStudio**
- The following R packages (install if needed):

```r
install.packages(c("dplyr", "ggplot2", "tidyr", "lubridate"))
